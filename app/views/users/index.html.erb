<h1>Index Users</h1>
<section class="user-list">
  <% if @users %>
    <% @users.each do |user| %>
      <%= render partial: "shared/user_card", locals: { user: user } %>
    <% end %>
  <% else %>
    <p>There's no users, please</p>
  <% end %>
</section>